[project]
name = "video_playground"
version = "0.1.0"
description = "Video pipeline playground"
requires-python = ">=3.14"

dependencies = [
    "opencv-python",
    "pytest",
]

[tool.pytest.ini_options]
pythonpath = [
    "."
]

testpaths = [
    "tests"
]